// swift-tools-version: 5.8

import PackageDescription

let package = Package(
    name: "stduritemplate",
    products: [
        .library(
            name: "stduritemplate",
            targets: ["stduritemplate"]),
    ],
    targets: [
        .target(
            name: "stduritemplate"),
    ]
)
